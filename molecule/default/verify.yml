---
# This is an example playbook to execute Ansible tests.

- name: Verify
  hosts: all
  tasks:
    - name: check if prometheus exporter is enabled
      shell: ss -tp state listening sport = :9854 | grep 'prometheus'
      register: results

    - name: See service returned
      debug: var=results
